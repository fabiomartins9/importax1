/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
}

module.exports = nextConfig

// next.config.js
module.exports = {
    images: {
      domains: ['i.imgur.com'],
    },
  };
  
